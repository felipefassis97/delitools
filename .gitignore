# Binarios compilados do Delitools (nao versionar builds, so codigo-fonte)
PrinterInstaller/Delitools.exe
PrinterInstaller/Delitools_Setup_*.exe
PrinterInstaller/Delitools_Portavel_*.zip

# Pasta de staging usada so pra montar o zip portatil (duplica NetConfigTools)
PrinterInstaller/DelitoolsPortavel/

# Configuracoes locais do Claude Code (especificas desta maquina)
.claude/settings.local.json

# URL de telemetria (endpoint de escrita) — nunca vai pro repositorio publico.
# Pega tambem a variante "telemetria.txt.txt" que o Notepad cria quando "ocultar
# extensoes de arquivos conhecidos" esta ativado no Windows (ja aconteceu uma vez).
telemetria.txt*
PrinterInstaller/telemetria.txt*
